# 更新日志

## [2.2.2] - 2026-09-12

### 热更新修复 (v2.2.0 -> v2.2.1 升级失败)
- 根因一: 更新器进程用 DETACHED_PROCESS 拉起 PowerShell, 会静默初始化
  失败直接退出, 更新什么都没做。现仅用 CREATE_NO_WINDOW
- 根因二: 更新器脚本的字符串续行语法错误导致整段解析失败。已新增
  PowerShell 语法解析门禁测试拦截
- 启动时自动恢复待应用更新: 上次「重启安装」未成功的下载不再被清空,
  下次打开程序会继续完成安装并重启
- 更新器全程写 _update/updater.log; 备份/替换全部纳入异常回滚

### 界面修复
- 「开始执行 / 停止 / 立即更新」主按钮紧凑化, 修复蓝色大按钮过长
- 更新对话框窄宽度下按钮不再重叠; 新增最小宽度回归测试

## [2.2.1] - 2026-09-12

### 露骨图片识别 (Shield) 逻辑审查与修复
- should_flag 阈值比较规范化, 消除同义级别别名排序歧义
- 打码输出文件名防覆盖 (递归扫描同名文件不再互相覆盖)
- 复用已检测区域, 每张风险图节省一次 censor 模型推理
- 打码失败追加明确告警; 报告失败原因不再误标

### 发行页整洁化
- SHA256 校验和从独立资产改为嵌入 Release 正文, 发行页仅保留安装包

## [2.2.0] - 2026-09-12

### 热更新 (方案 A: 整包自更新)
- 启动 2.5s 后静默检查新版本 (GitHub Releases API, 仅正式版通道), 24h 一次;
  侧边栏底部与「关于 → 检查更新」菜单提供手动入口
- 更新对话框: 新版本号/更新日志/下载进度/SHA256 校验, 支持「跳过此版本」
- 下载支持断点续传与取消; 磁盘空间预检; 校验和缺失时优雅降级
- PowerShell 更新器: 等主程序退出 → 旧版备份 (_backup) → 替换 exe 与
  _internal → 自动重启; 替换失败自动回滚, 用户配置 (config.ini 等) 天然保留
- 仅替换 exe 与 _internal, exe 文件名不进入更新路径 (中文名/英文名均兼容)
- CI 发布流程附带 SHA256 校验和资产 (build.yml / build-beta.yml)
- 测试环境可用 XIAOXUE_NO_UPDATE_CHECK=1 禁用自动检查

### 配置存储修复
- 修复 QSettings 误用双参构造导致配置写入 Windows 注册表的问题
  (setDefaultFormat 对该构造器无效), 现统一为便携式 config.ini
  (exe / 项目根目录), 并清理了测试污染的注册表残留

### 启动程序改名
- 主程序文件名改为中文「小雪工具箱.exe」，更符合软件定位
- 经评估不影响兼容模式 (AviSynth + VSFilter) 运行: exe 文件名不进入
  AVS/FFmpeg 路径链路, 「非中文路径」要求针对安装目录, 保持不变
- 发行包文件名保持英文 (XiaoXueToolbox_*.zip), 兼顾下载兼容性


## [2.1.0] - 2026-09-12

### 界面深度优化 (UI Refresh)

### 主题系统
- 设计令牌化重构: 全部颜色/字体/圆角收敛到 `PALETTES` 令牌表, QSS 由单一模板生成
- 新增暗色主题 (Fluent Dark 规范), 菜单栏「视图」或侧边栏底部一键切换, 即时生效并持久化
- `apply_theme` 同步 QPalette, 保证未覆盖控件在暗色下可读
- 统一提示色: 轨道面板/帮助页原先混用的 Material 色值全部并入主题令牌

### 导航与品牌
- 侧边栏新增品牌区 (Logo + 名称 + 版本号, Logo 随主题自动切换)
- 12 个功能页按「视频处理 / 格式与媒体 / 质量与批量 / 特色与辅助 / 帮助」分组
- 全套图标系统 (qtawesome): 侧边栏/执行/停止/清空/菜单, 选中态随强调色换色
- 窗口几何与最大化状态持久化, 最小尺寸 960x640, 首次启动居中
- 重设计关于对话框 (Logo/版本/许可/第三方组件/链接)

### 表单与页面
- 表单内容列限宽 860px 居中, 宽窗口下不再被拉长
- 分组标题内嵌卡片; 提示条/说明文字语义化样式随主题联动
- 文件拖放时输入框强调色高亮反馈
- 视频压制页「高级选项」改为可折叠面板
- 帮助页内嵌 HTML 配色随主题联动

### 执行区与日志
- 日志面板新增工具条: 关键字过滤 / 自动滚动 / 自动换行 / 复制全部 / 清空
- 日志支持 Ctrl+滚轮调整字号, 空状态引导文案
- 指标卡数值改用等宽字体, 避免跳动
- 移除整页透明度切换动画 (复杂页面 Windows 上易掉帧), 改纯位移滑入
- 新增「减少动画」开关; Ctrl+Return 快捷执行

### 视觉升级 (第二轮)
- 卡片去蓝色左条, 统一 10px 圆角细描边 (WinUI 卡片风格), 标题内嵌
- 底部按钮区/进度仪表盘/日志区与页面内容列同宽居中 (像素级对齐)
- 页面滚动条常驻细轨, 视口宽度稳定以支撑对齐

### 测试覆盖率 100%
- 新增 `tools/test_coverage.sh`: 每个测试文件独立进程运行 + `--cov-append` 合并,
  `coverage report --fail-under=100` 强制 100% 覆盖率门槛
- 移除无引用的 Gooey 遗留模块 `src/gui_tabs.py`
- 清理 `core.py` / `compat_encoder.py` 中永不可触发的死代码 try/except
- 修复批量补丁引入的 remux 轨道面板属性名错误 (槽内异常会导致进程 abort)
- 新增约 300 个测试: UI 全组件 (离屏渲染)、命令构建矩阵、执行器编排、
  通知发送 (mock requests)、NSFW 检测 (注入假 imgutils 模块)

### 打包与依赖
- 新增依赖: qtawesome (图标库)
- PyInstaller spec 打入 qtawesome 字体数据与 image 品牌资源


## [2.0.2] - 2026-04-25

### UI 现代化 (Fluent Design 风格)

#### 视觉主题全面升级
- 采用 Windows 11 Fluent Design 设计语言重写 QSS 主题
- 全局 8px 圆角、卡片式 GroupBox (左侧蓝色色条 + 轻阴影)
- 输入控件 Fluent TextBox 风格：底部 2px 重点线，聚焦时变为蓝色
- 按钮区分主操作 (蓝色 Accent)、停止 (红色 Danger)、清空 (透明次要) 三种样式
- 侧边栏 WinUI NavigationView 风格，选中项白色背景 + 蓝色文字

#### 侧边栏药丸形指示器
- 新增 `_PillIndicator` 组件：4px 宽蓝色圆角竖条，随选中项平滑滑动
- 使用 `QPropertyAnimation` 实现丝滑的 Y 轴位移过渡

#### 页面切换动画
- 新增 `AnimatedStackedWidget` 组件：切换功能页面时带水平滑动 + 淡入过渡
- 左右方向自适应（向前/向后切换方向不同）
- 使用 `QParallelAnimationGroup` 并行运行滑动 + 透明度动画

#### 进度仪表盘
- 新增 `ProgressDashboard`：4 个指标卡片（进度、速度、帧、剩余时间）+ 渐变色进度条
- 新增 `FFmpegProgressParser`：实时解析 FFmpeg stderr 输出提取 frame/fps/time/speed 等指标
- 任务未运行时仪表盘自动隐藏，任务开始时显示不确定进度，获取数据后实时更新

#### 日志面板美化
- 日志面板改为深色终端风格 (`#1e1e1e` 背景，Cascadia Code 字体)
- ANSI 颜色映射调整为深色背景适配的高对比度色值
- 新增平滑滚动效果，使用 `QPropertyAnimation` 缓动到底部

#### 按钮与状态栏动画
- 执行按钮点击时带微缩弹回动画 (`button_press_anim`)
- 任务完成/失败时状态栏颜色短暂闪烁反馈 (`status_flash`)

### Bug 修复
- 修复日志面板滚动动画使用 `DeleteWhenStopped` 导致悬空 C++ 引用，压制开始后崩溃的问题
- 修复 QSS 中 `font-size: 0px` 导致的 `QFont::setPixelSize: Pixel size <= 0` 警告
- 日志面板默认高度加高，分割器比例调整，日志区域更加宽敞

### 新增文件
- `src/ui/animations.py` — 可复用动画工具集 (淡入、页面滑动、按钮脉冲、状态闪烁)
- `src/ui/ffmpeg_progress.py` — FFmpeg 输出进度解析器
- `src/ui/progress_dashboard.py` — 进度仪表盘 UI 组件

---

## [2.0.0] - 2026-04-25

### 重大更新: GUI 框架迁移 Gooey → PyQt6

本版本将图形界面框架从 Gooey (wxPython) 完全迁移至 **PyQt6**，实现了更灵活的 UI 布局、动态控件联动和异步任务管理。

#### 新架构
- 新增 `src/ui/` 模块：`main_window.py` 主窗口、`sidebar.py` 侧边栏、`log_panel.py` 日志面板、`task_runner.py` 异步任务
- 新增 `src/ui/tabs/` 模块：12 个独立功能标签页，各自包含 `build_args()` 方法构建兼容参数
- 新增 `src/ui/base_tab.py`：通用控件创建辅助方法基类
- 新增 `src/ui/args_builder.py`：`ArgsNamespace` 兼容 `argparse.Namespace`，后端执行器无需修改
- 新增 `src/ui/theme.py`：QSS 统一主题样式

#### 封装转换自动识别轨道
- 选择输入文件后自动调用 `ffprobe` 探测音频/字幕轨道
- 以复选框形式展示每条轨道的编解码器、声道、语言、码率等详情
- 用户直接勾选/取消即可选择保留哪些轨道，无需手动查看编号
- 批量模式下自动显示第一个文件的轨道信息

#### 容器兼容性警告
- 封装转换时自动检测源文件编码与目标容器的兼容性
- 不兼容时在界面中显示警告并给出建议 (如 "VP9 不被 MP4 容器支持，建议改用 MKV")
- 覆盖 MP4、WEBM、MOV、AVI、TS 等主流容器的兼容性规则

#### 动态控件联动
- 视频压制：非自定义预设时自动禁用编码器/质量参数编辑；NVENC 档位仅在选择 NVENC 编码器时可用
- 视频压制：音轨/字幕自定义编号输入框仅在选择「自定义选择」时启用
- 视频压制：分发目录仅在分发模式非「不分发」时可编辑
- 封装转换：自定义后缀名输入框仅在选择「自定义」预设时启用
- 图片转换：自定义格式输入框仅在选择「自定义」时启用；质量滑块仅对 JPG/WEBP 有效
- 素材质量检测：自定义分辨率输入框仅在选择「自定义」时启用
- 批量重命名：输出路径仅在复制/移动模式时可编辑
- 露骨图片识别：打码相关控件仅在启用打码时可操作；自定义图片仅在选择自定义模式时启用

#### 内联帮助提示
- 新增 `add_hint()` 方法，支持信息 (蓝)、警告 (橙)、提示 (绿) 三种样式
- 所有功能页面增加来自 `gui_tabs.py` 的关键操作说明，直接可见而非仅作为 tooltip
- 使用说明页面重写为实时渲染的 HTML 帮助文档

#### 通用增强
- 所有文件输入框支持拖放 (FileDropLineEdit)
- ANSI 彩色日志渲染支持
- 版本号改为从 git tag 自动读取，CI 构建时注入 (`src/_version.py`)
- 更新 GitHub Actions 工作流以适配新版本注入方式

### 依赖变更
- 移除: `Gooey>=1.0.8`
- 新增: `PyQt6>=6.5.0`

### 视频压制增强

#### 输出格式选择
- 新增输出容器格式选项：MP4 (默认)、MKV、MOV、与输入相同、自定义
- 自定义模式支持手动输入任意扩展名 (如 ts、webm)
- 输出格式影响自动生成的输出路径扩展名

#### 更多质量预设
- 新增「较小体积」x264 CRF22 预设，适合对画质要求不高、需要快速出片的场景
- 新增「均衡画质」x265/HEVC CRF20 预设，同画质下体积约为 x264 的 60-70%
- 重命名现有预设以更清晰地标注编码器和参数

#### 音频/字幕轨道自动检测
- 视频压制模块选择输入文件后自动调用 `ffprobe` 探测音频/字幕轨道
- 以复选框形式展示每条轨道的详细信息 (编解码器、声道、语言、码率)
- 音频轨道默认全选，字幕轨道默认不选
- 有探测数据时自动隐藏静态下拉选择，无探测数据时自动回退至静态模式

#### 视频输出说明
- 视频输出区域新增提示：留空分辨率和帧率时保持原始值，不做缩放或变速处理

### CI/CD 增强

#### Release Notes 动态生成
- GitHub Release 说明不再使用静态文本，自动从 CHANGELOG.md 解析当前版本的更新日志
- 同时适用于正式版 (build.yml) 和预发布版 (build-beta.yml) 的工作流
- 构建矩阵提供两版本：标准版、Shield 增强版


---

## [1.9.0] - 2026-04-13

### 新增功能

#### 媒体元数据检测
- 新增「**媒体元数据检测**」独立功能标签页
- 通过 `ffprobe` 探测视频、音频、图片的完整流信息
- 报告中标注每条音频流和字幕流的编号（`#0`, `#1`...）及语言标签（`jpn`/`chi`...）
- 多轨道文件底部自动附带使用提示，引导用户在压制/封装转换中精确引用
- 支持单文件/多文件选择和目录扫描（可选递归子目录）两种输入模式
- 支持将报告输出为 TXT 文件

#### 音轨/字幕精确选择（视频压制 & 封装转换）
- 「视频压制」新增音轨选择：快捷选择（全部/仅第一条/不保留）+ 自定义编号输入
- 「视频压制」新增字幕流选择：支持保留内封字幕流，可精确指定保留对应编号
- 「封装转换」新增流选择分组，音轨和字幕流均支持快捷模式和自定义模式
- 「封装转换」新增封装转换目标格式用户自定义输入
- 精确编号输入支持多条逗号分隔，如 `0,2` 表示保留 `#0` 和 `#2` 两条流
- 使用 FFmpeg 的可选映射语法（`0:a:N?`），当源文件对应流不存在时不报错

---

## [1.8.2] - 2026-04-13

### 改进
- 「封装转换」新增封装转换目标格式用户自定义输入

---

## [1.8.1] - 2026-03-17


### Bug修复
修复以下复选框无效的问题：
- 素材质量检测 的 PR 视频兼容性、PR 图片兼容性
- 图片转换 的 忽略同格式文件
- 文件夹创建 的 自动排序
- 批量重命名 的 排除下划线后文字
- 露骨图片识别 的 递归扫描子目录

---

## [1.8.0] - 2026-03-17

### 音视频抽取功能增强
- 新增视频抽取功能，现在可以实现视频文件的音视频分别分离

### 跳过同类型图片转换
- 自动检测输出目录中是否已存在同类型文件，存在则跳过转换

---

## [1.7.0] - 2026-02-19

### 批量重命名增强
- **排序规则自定义**: 新增排序设置区块，可选择按文件名或文件大小排序
- **排序方向控制**: 支持升序（从小到大）和降序（从大到小）
- **关键词提前排序**: 新增关键词提前功能，文件名包含特定关键词（如“主视觉图”）的文件将优先排列在序列首部

### 压制后成品分发
- **自动化分发**: 新增压制结束后自动分发功能，可将成品文件复制或移动到指定目录
- **挂载支持**: 配合 WebDAV、SMB 等网络位置挂载，可实现自动上传成品到 NAS 或云端

### GUI 体验优化
- **拖拽支持说明**: 输入框提示语更新，明确支持将文件或文件夹直接拖动到路径输入框内

---

## [1.6.0] - 2026-01-11

### 代码重构
- **代码结构优化**: 优化代码结构，提高代码可读性和可维护性

---

## [1.5.2] - 2026-01-11

### 兼容模式修复
- **字幕兼容模式**: 添加兼容模式需要运行在非中文路径下的提醒


## [1.5.1] - 2026-01-11

### Shield 功能增强
- **打码范围扩展**: 新增 `expand_pixels` 参数，可向外扩展打码区域 (0-200 像素)
- **多种打码模式**: 马赛克、模糊、黑色遮盖、表情覆盖、自定义图片
- **处理强度通用化**: 处理强度/大小参数对所有模式生效
  - 马赛克: 方块大小
  - 模糊: 半径
  - 黑色遮盖: 圆角大小
  - 表情: 元素比例
- **评级修复**: 添加 `r15`/`r18` 评级映射，修复 unknown 状态
- **报告优化**: 统计 → 详情 → 处理结果，三段式结构
- **RGBA 兼容**: 自动转换 RGBA 为 RGB，修复 JPEG 保存错误

---

## [1.5.0] - 2026-01-11

### 新增功能
- **露骨图片识别 (Shield)**: 基于 AI 的 NSFW 内容检测
  - 识别 B 站投稿过审风险图片
  - 支持 4 级风险分类: safe/sensitive/questionable/explicit
  - 可自定义风险阈值
  - 支持自动马赛克/模糊打码
  - 生成详细检测报告
- **双版本构建**: GitHub Actions 自动构建两个版本
  - 标准版: 体积较小，不含 AI 模块
  - Shield 增强版: 包含露骨图片识别功能

### 技术改进
- 新增 `src/nsfw_detect.py` 核心检测模块
- 使用条件导入实现功能可选
- Matrix 策略并行构建双版本

### 依赖更新 (Shield 版)
- dghs-imgutils >= 0.8.0
- onnxruntime >= 1.15.0

---

## [1.4.0] - 2026-01-11

### 新增功能
- **Docs**:  在顶部菜单栏添加“帮助文档”跳转菜单；在各功能页**底部**添加独立的“在线文档”复制框。
- **字幕兼容模式**: AviSynth + VSFilter 字幕渲染
  - 解决 Windows 系统字体无法正确加载、字重无法匹配等问题
  - 便携版设计，无需安装 AviSynth，解压即用
  - 复用所有现有编码参数
- **NVENC 速度档位控制**: 新增 p1-p7 档位选择
  - 预设模式和自定义模式均可调节
  - 选择"使用预设默认"则使用预设定义的档位
- **编码参数重构**: 使用 `EncodeParams` 数据类统一管理
  - 输入验证 (文件存在、CRF 范围、分辨率格式)
  - 更清晰的代码结构

### 改进
- **默认音频编码**: 改为"复制 (不重新编码)"
- **预设配置完善**: 显式添加 `audio_encoder` 字段
- **测试覆盖增强**: 39 个单元测试用例

### 依赖更新
- 新增 pytest (测试框架)

---

## [1.3.0] - 2026-01-11

### 新增功能
- **字幕兼容模式**: AviSynth + VSFilter 字幕渲染
  - 解决 Windows 系统字体无法正确加载、字重无法匹配等问题
  - 便携版设计，无需安装 AviSynth，解压即用
  - 复用所有现有编码参数
- **NVENC 速度档位控制**: 新增 p1-p7 档位选择
  - 预设模式和自定义模式均可调节
  - 选择"使用预设默认"则使用预设定义的档位
- **编码参数重构**: 使用 `EncodeParams` 数据类统一管理
  - 输入验证 (文件存在、CRF 范围、分辨率格式)
  - 更清晰的代码结构

### 改进
- **默认音频编码**: 改为"复制 (不重新编码)"
- **预设配置完善**: 显式添加 `audio_encoder` 字段
- **测试覆盖增强**: 39 个单元测试用例

### 依赖更新
- 新增 pytest (测试框架)

---

## [1.2.3] - 2026-01-11

### 新增功能
- **NVIDIA 显卡加速 (HQ) 预设**: 画质优先，CQ 19 + P7 + VBR
- **Debug 模式**: 仅输出 FFmpeg 命令，不执行
- **日志增强**: FFmpeg 命令记录到日志文件

### 改进与修复
- **严格模式**: 非自定义模式强制忽略 GUI 参数设置
- **NVENC 参数修复**: 修复 cq 参数可能丢失问题
- **预设优化**: NVENC 预设添加 VBR 模式

---

## [1.2.0] - 2026-01-10

### 新增功能
- 增加图片文件头检测
- 增加版本号显示

### 改进
- 修复图片扫描 bug
- 自动更新 GUI 版本号
- 增加硬件编码检测
- 新增 logo

---

## [1.1.0] - 2026-01-10

### 新增功能
- 图片格式转换
- 批量创建文件夹
- 批量序列重命名
- 封装转换批量模式
- 通知设置 (飞书/Webhook)

### 改进
- 通知配置回显和管理
- 素材质量检测自定义规则
- 封装转换新增预设

### 依赖更新
- 新增 Pillow、chardet

---

## [1.0.0] - 2026-01-09

### 首个正式版本
- 视频压制 (H.264/H.265，字幕烧录)
- 音频替换
- 封装转换 (Remux)
- 质量检测 (QC)
- 音频抽取